apiVersion: v2
name: git-cdn
description: Helm chart for bpaquet/git_cdn - a git+http(s) caching proxy with built-in aiohttp retry (0.1s→51.2s ×10) and pack cache; designed for CI worker acceleration when upstream (github.com) is flaky
version: 1.0.0
appVersion: "master"
maintainers:
  - name: platform-team
